# connect to the timescaledb container (CORRECTED)
docker exec -it batch-timescaledb psql -U condvest_user -d condvest

# down the container (CORRECTED)
docker-compose -f aws_lambda_architecture/batch_layer/local_dev/docker-compose.yml down

# Alternative: if running from batch_layer/local_dev directory
# docker-compose down
