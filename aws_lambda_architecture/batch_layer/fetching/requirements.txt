# Fetching Component Dependencies - Lambda Function Code Only
# Heavy dependencies will be provided via Lambda Layers

# Core dependencies (boto3 is already available in Lambda runtime)
requests>=2.28.0
python-dateutil>=2.8.0

# Data validation and models
pydantic>=2.0.0,<3.0.0

# PostgreSQL client (lightweight version) 
psycopg2-binary>=2.9.0
